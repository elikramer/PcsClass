'use strict'

function everyArray_Function(array, callback){
    for(let i = 0; let < array1.length; i++){
        if(callback(theArray[i]) != true){
            return false;
        }
    }
    return true;
}

function test (letter){
    let pass = false;
    if(letter === letter.toUpperCase()){
        pass = true;
    }
    else{
        pass = false;
    }
}
